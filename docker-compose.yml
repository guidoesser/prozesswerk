services:
  prozesswerk:
    build: .
    container_name: prozesswerk
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_BASE_URL=${LLM_BASE_URL}
      - LLM_MODEL=${LLM_MODEL}
    restart: unless-stopped
